import styled from "styled-components";

type TMain = {
}

export const CountdownStyled = styled.div<TMain>`
`
export const DefaultRendererStyled = styled.div`
  display: flex;
  p {
    font-weight: 500;
    font-size: 14px;
    margin: 0;
  }
`